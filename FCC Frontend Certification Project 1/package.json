{
  "name": "fcc-frontend-certification-project-1",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:sass": "sass SCSS:public",
    "watch:sass": "sass SCSS:public --watch",
    "build:vite": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "start": "npm-run-all --parallel build:sass watch:sass dev",
    "build": "npm-run-all --parallel build:sass build:vite"

  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.69.5"
  }
}
